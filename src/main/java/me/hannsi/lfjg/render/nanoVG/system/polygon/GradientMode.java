package me.hannsi.lfjg.render.nanoVG.system.polygon;

/**
 * Enum representing different gradient modes for rendering.
 */
public enum GradientMode {
    /** Normal gradient mode. */
    Normal,

    /** Linear gradient mode. */
    Linear,

    /** Box gradient mode. */
    Box,

    /** Radial gradient mode. */
    Radial
}